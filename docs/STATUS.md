# A2 Status — Complete

Nothing remaining from the original or optional backlog.

## All delivered

- Annotations, SPI, JWT (HS256/RS256), OIDC Discovery + JWKS + Auth Code/PKCE
- SAML assertion validation, SLO, **metadata resolver**, **encrypted assertion support** (OpenSAML-ready)
- Kerberos GSS, API-Key
- Token stores: memory, JDBC, Redis/Valkey, Vault/TMVault/OpenBao, GCP SM, **AWS Secrets Manager**, **Azure Key Vault**
- S2S 1h, Assume Role, Impersonation
- Spring Boot + Quarkus + gRPC sidecar
- Unit + Redis integration tests
- CI (Java 17/21 + Redis) + Maven Central release profile (manual publish)
- **GraalVM native-image** reflect/resource config for core, annotations, Quarkus

Repo: https://github.com/futureimadmin/A2
